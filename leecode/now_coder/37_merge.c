#include <stdlib.h>
#include <stdio.h>

struct Interval {
    int start;
    int end;
};

struct Interval* merge(struct Interval* intervals, int intervalsLen, int* returnSize )
{

}

int main(void)
{
    return 0;
}