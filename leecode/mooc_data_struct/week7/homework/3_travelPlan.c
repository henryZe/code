

int main(void)
{
    int v_num, e_num, src, dest;
    scanf("%d %d %d %d\n", &v_num, &e_num, &src, &dest);

    int v, w, dist, cost;
    for (int i = 0; i < e_num; i++) {
        scanf("%d %d %d %d\n", &v, &w, &dist, &cost);
    }

    return 0;
}
